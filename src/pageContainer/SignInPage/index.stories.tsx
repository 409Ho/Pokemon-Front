import SingInPage from '.';

import { Meta, StoryObj } from '@storybook/react';

export default {
  title: 'SignInPage',
  component: SingInPage,
} as Meta<typeof SingInPage>;

type Story = StoryObj<typeof SingInPage>;

export const Primary: Story = {};
