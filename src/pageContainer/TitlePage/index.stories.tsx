import TitlePage from '.';

import { Meta, StoryObj } from '@storybook/react';

export default {
  title: 'TitlePage',
  component: TitlePage,
} as Meta<typeof TitlePage>;

type Story = StoryObj<typeof TitlePage>;

export const Primary: Story = {};
